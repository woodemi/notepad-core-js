import NotepadCommand from "../NotepadCommand.js";

export class WoodemiCommand extends NotepadCommand {
    constructor(request, intercept, handle) {
        super(request, intercept, handle);
    }
}
